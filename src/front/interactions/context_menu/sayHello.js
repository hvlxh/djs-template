/** @param {import('hvlxh').UserContextRun} param0 */
export default ({ interaction }) => {
  interaction.reply('Hello!')
}

/** @type {import('hvlxh').UserContext} */
export const info = {
  name: 'Say hello',
  type: "user"
}